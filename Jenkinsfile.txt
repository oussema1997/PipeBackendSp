pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                script {
                     sh 'mvn clean install'
                }
            }
        }

 stage('Deploy') {
            steps {
                script {
                    
nexusArtifactUploader artifacts: [[artifactId: 'DevOps_Project', classifier: '', file: 'artifact', type: 'jar']], credentialsId: '', groupId: 'pom.groupId', nexusUrl: '192.168.33.15:8081', nexusVersion: 'nexus3', protocol: 'http', repository: 'maven-releases', version: 'pom.version' 
                }
            }
        }
    }
    
}